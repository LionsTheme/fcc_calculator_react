import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

/**
 * Renders the App component in the root element of the document.
 */
ReactDOM.render(<App />, document.getElementById("root"));
